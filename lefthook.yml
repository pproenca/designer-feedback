commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-commit:
  commands:
    format:
      run: npm run format

pre-push:
  parallel: false
  commands:
    typecheck:
      run: npm run typecheck
    lint:
      run: npm run lint
    test:
      run: npm run test
